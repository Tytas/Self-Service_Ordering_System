<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.layout.*?>
<?import javafx.scene.control.*?>
<?import javafx.geometry.Insets?>

<BorderPane xmlns:fx="http://javafx.com/fxml"
            fx:controller="com.app.controller.MainController"
            prefWidth="800" prefHeight="600"
            style="-fx-background-color: #f5f5f5;">

    <!-- ═══════════════ HEADER ═══════════════ -->
    <top>
        <VBox style="-fx-background-color: #2c3e50; -fx-padding: 16;">
            <Label text="🍔 Food Order"
                   style="-fx-text-fill: white; -fx-font-size: 22px; -fx-font-weight: bold;"/>
            <Label text="Composez votre commande"
                   style="-fx-text-fill: #bdc3c7; -fx-font-size: 13px;"/>
        </VBox>
    </top>

    <!-- ═══════════════ CONTENU PRINCIPAL ═══════════════ -->
    <center>
        <HBox spacing="16">
            <BorderPane.margin>
                <Insets top="16" right="16" bottom="16" left="16"/>
            </BorderPane.margin>

            <!-- ─── Panneau gauche : catalogue ─── -->
            <VBox spacing="12" prefWidth="340"
                  style="-fx-background-color: white; -fx-background-radius: 8; -fx-padding: 16;">

                <Label text="Catalogue"
                       style="-fx-font-size: 16px; -fx-font-weight: bold;"/>

                <!-- Onglets Snacks / Drinks -->
                <TabPane fx:id="catalogTabPane" tabClosingPolicy="UNAVAILABLE">

                    <Tab text="Snacks">
                        <VBox spacing="8" style="-fx-padding: 12;">
                            <!-- Chaque ligne = un produit + bouton Ajouter -->
                            <HBox alignment="CENTER_LEFT" spacing="8">
                                <Label text="🍔 Burger" style="-fx-font-size: 14px;" HBox.hgrow="ALWAYS"/>
                                <Label text="? ₺" style="-fx-text-fill: #7f8c8d;"/>
                                <Button text="+" onAction="#onAddSnack"
                                        userData="burger"
                                        style="-fx-background-color: #27ae60; -fx-text-fill: white; -fx-background-radius: 4;"/>
                            </HBox>
                            <HBox alignment="CENTER_LEFT" spacing="8">
                                <Label text="🥪 Panini" style="-fx-font-size: 14px;" HBox.hgrow="ALWAYS"/>
                                <Label text="? ₺" style="-fx-text-fill: #7f8c8d;"/>
                                <Button text="+" onAction="#onAddSnack"
                                        userData="panini"
                                        style="-fx-background-color: #27ae60; -fx-text-fill: white; -fx-background-radius: 4;"/>
                            </HBox>
                        </VBox>
                    </Tab>

                    <Tab text="Drinks">
                        <VBox spacing="8" style="-fx-padding: 12;">
                            <HBox alignment="CENTER_LEFT" spacing="8">
                                <Label text="☕ Coffee" style="-fx-font-size: 14px;" HBox.hgrow="ALWAYS"/>
                                <Label text="? ₺" style="-fx-text-fill: #7f8c8d;"/>
                                <Button text="+" onAction="#onAddDrink"
                                        userData="coffee"
                                        style="-fx-background-color: #2980b9; -fx-text-fill: white; -fx-background-radius: 4;"/>
                            </HBox>
                            <HBox alignment="CENTER_LEFT" spacing="8">
                                <Label text="💧 Water" style="-fx-font-size: 14px;" HBox.hgrow="ALWAYS"/>
                                <Label text="? ₺" style="-fx-text-fill: #7f8c8d;"/>
                                <Button text="+" onAction="#onAddDrink"
                                        userData="water"
                                        style="-fx-background-color: #2980b9; -fx-text-fill: white; -fx-background-radius: 4;"/>
                            </HBox>

                            <Separator/>

                            <!-- Options décorateur -->
                            <Label text="Options"
                                   style="-fx-font-weight: bold; -fx-font-size: 13px;"/>
                            <CheckBox fx:id="sugarCheck" text="+ Sucre"/>
                            <CheckBox fx:id="cremeCheck" text="+ Crème"/>
                        </VBox>
                    </Tab>

                </TabPane>
            </VBox>

            <!-- ─── Panneau droit : commande en cours ─── -->
            <VBox spacing="12" HBox.hgrow="ALWAYS"
                  style="-fx-background-color: white; -fx-background-radius: 8; -fx-padding: 16;">

                <Label text="Ma commande"
                       style="-fx-font-size: 16px; -fx-font-weight: bold;"/>

                <!-- Liste des articles ajoutés -->
                <ListView fx:id="orderListView" VBox.vgrow="ALWAYS"
                          style="-fx-background-radius: 6;"/>

                <Separator/>

                <!-- Total -->
                <HBox alignment="CENTER_RIGHT" spacing="8">
                    <Label text="Total :" style="-fx-font-weight: bold; -fx-font-size: 14px;"/>
                    <Label fx:id="totalLabel" text="0 ₺"
                           style="-fx-font-size: 14px; -fx-text-fill: #27ae60; -fx-font-weight: bold;"/>
                </HBox>

                <!-- Actions -->
                <HBox spacing="8" alignment="CENTER_RIGHT">
                    <Button fx:id="clearButton" text="Vider" onAction="#onClearOrder"
                            style="-fx-background-color: #e74c3c; -fx-text-fill: white; -fx-background-radius: 4; -fx-padding: 8 16;"/>
                    <Button fx:id="orderButton" text="Commander" onAction="#onConfirmOrder"
                            style="-fx-background-color: #2c3e50; -fx-text-fill: white; -fx-background-radius: 4; -fx-padding: 8 16; -fx-font-weight: bold;"/>
                </HBox>

            </VBox>
        </HBox>
    </center>

    <!-- ═══════════════ STATUS BAR ═══════════════ -->
    <bottom>
        <VBox style="-fx-background-color: #ecf0f1;">
            <ProgressBar fx:id="progressBar" 
                        progress="0.0"
                        prefWidth="Infinity"
                        style="-fx-accent: #27ae60;"/>
            <Label fx:id="statusLabel" text="Prêt"
                style="-fx-padding: 6 16; -fx-font-size: 12px; -fx-text-fill: #7f8c8d;"/>
        </VBox>
    </bottom>

</BorderPane>
